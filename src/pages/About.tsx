import Section from '@/components/Section'

export default function About() {
  return (
    <Section title="Studio">
      <p className="type-lede text-ink-muted">
        Placeholder — replace with the studio story.
      </p>
    </Section>
  )
}
